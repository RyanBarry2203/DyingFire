<Window x:Class="DyingFire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DyingFire.ViewModels"
        Title="Dying Fire" Height="720" Width="1080" 
        Background="#050505"
        Cursor="None"
        FontFamily="Gabriola">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <ImageBrush x:Key="WoodBrush" ImageSource="/Assets/Images/stylized_wood.png" TileMode="Tile" Viewport="0,0,100,100" ViewportUnits="Absolute" Opacity="0.6"/>

        <LinearGradientBrush x:Key="BronzeBorderBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E6C898" Offset="0.0"/>
            <GradientStop Color="#B38B54" Offset="0.4"/>
            <GradientStop Color="#684824" Offset="0.7"/>
            <GradientStop Color="#E6C898" Offset="1.0"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="BoneBrush" Color="#C8C0B0"/>
        <SolidColorBrush x:Key="BloodBrush" Color="#660000"/>
        <SolidColorBrush x:Key="SanityBrush" Color="#2A3D45"/>
        <DropShadowEffect x:Key="GhostGlow" Color="#C8C0B0" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>

        <SolidColorBrush x:Key="SlotBackground" Color="#D9080808"/>

        <Style x:Key="HorrorScrollThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{StaticResource BronzeBorderBrush}" CornerRadius="3" Margin="2" Opacity="0.7"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="BorderBrush" Value="#222"/>
            <Setter Property="BorderThickness" Value="1,0,0,0"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource HorrorScrollThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HorrorBtn" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource BoneBrush}"/>
            <Setter Property="FontFamily" Value="Gabriola"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Cursor" Value="None"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{StaticResource SlotBackground}" 
                                    BorderBrush="{StaticResource BronzeBorderBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="2">
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E6C898"/>
                                <Setter TargetName="border" Property="Background" Value="#333"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Effect" Value="{StaticResource GhostGlow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BronzeOrb" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="None"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="#EE000000" Stroke="{StaticResource BronzeBorderBrush}" StrokeThickness="2"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{StaticResource BoneBrush}" TextBlock.FontSize="28"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Effect" Value="{StaticResource GhostGlow}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="GrimSlotTemplate">
            <Button Command="{Binding DataContext.SelectQuickSlotCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
        CommandParameter="{Binding}" 
        Background="Transparent" BorderThickness="0" Margin="6" Cursor="None"
        FocusVisualStyle="{x:Null}">
                <Grid>
                    <Border Width="60" Height="60" 
                    Background="#111111" 
                    BorderBrush="#333333" 
                    BorderThickness="1" 
                    CornerRadius="2">

                        <TextBlock Text="{Binding Name}" 
                           Foreground="#888" 
                           FontFamily="Palatino Linotype" 
                           FontSize="12" 
                           TextWrapping="Wrap" 
                           TextAlignment="Center" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"/>
                    </Border>
                    <Border x:Name="SelectBorder" BorderThickness="2" BorderBrush="{Binding BorderColor}" CornerRadius="2">
                        <Border.Effect>
                            <DropShadowEffect Color="#B38B54" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                        </Border.Effect>
                    </Border>
                </Grid>
            </Button>
        </DataTemplate>

    </Window.Resources>

    <Grid MouseMove="Grid_MouseMove">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="140"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Viewbox Stretch="Uniform">
                <Grid Width="900" Height="550">

                    <Image Source="{Binding BackgroundImage}" Stretch="UniformToFill"/>

                    <Border BorderBrush="{StaticResource BronzeBorderBrush}" BorderThickness="3" IsHitTestVisible="False">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="25"/>
                        </Border.Effect>
                    </Border>

                    <ItemsControl ItemsSource="{Binding CurrentLocation.Interactables}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="{Binding Width}" Height="{Binding Height}" 
            Command="{Binding DataContext.InteractCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
            CommandParameter="{Binding}"
            Cursor="None" Background="Transparent" BorderThickness="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid Background="Transparent">
                                                <Border x:Name="HoverPrompt" 
                            Background="#E6050505" 
                            BorderBrush="{StaticResource BronzeBorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="3" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center" 
                            Padding="15,5"
                            Opacity="0">
                                                    <Border.Effect>
                                                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2"/>
                                                    </Border.Effect>

                                                    <TextBlock Text="{Binding ActionText}" 
           Foreground="{StaticResource BoneBrush}" 
           FontFamily="Gabriola" 
           FontSize="28"/>

                                                </Border>
                                            </Grid>

                                            <ControlTemplate.Triggers>
                                               


                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="HoverPrompt" 
                                                     Storyboard.TargetProperty="Opacity" 
                                                     To="1" Duration="0:0:0.2"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="HoverPrompt" 
                                                     Storyboard.TargetProperty="Opacity" 
                                                     To="0" Duration="0:0:0.2"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button Panel.ZIndex="100" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Content="▲" Style="{StaticResource BronzeOrb}" Command="{Binding MoveCommand}" CommandParameter="North" Visibility="{Binding CanMoveNorth, Converter={StaticResource BoolToVis}}"/>
                    <Button Panel.ZIndex="100" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" Content="▼" Style="{StaticResource BronzeOrb}" Command="{Binding MoveCommand}" CommandParameter="South" Visibility="{Binding CanMoveSouth, Converter={StaticResource BoolToVis}}"/>
                    <Button Panel.ZIndex="100" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Content="◄" Style="{StaticResource BronzeOrb}" Command="{Binding MoveCommand}" CommandParameter="West" Visibility="{Binding CanMoveWest, Converter={StaticResource BoolToVis}}"/>
                    <Button Panel.ZIndex="100" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0" Content="►" Style="{StaticResource BronzeOrb}" Command="{Binding MoveCommand}" CommandParameter="East" Visibility="{Binding CanMoveEast, Converter={StaticResource BoolToVis}}"/>

                    <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30" 
                            Content="Stop Hiding" Width="150" Height="45" Style="{StaticResource HorrorBtn}" 
                            Command="{Binding StopHidingCommand}"
                            Visibility="{Binding IsHidingUI, Converter={StaticResource BoolToVis}}"/>
                </Grid>
            </Viewbox>
        </Grid>

        <Border Grid.Row="1" Background="Black" BorderBrush="{StaticResource BronzeBorderBrush}" BorderThickness="0,2,0,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="90" ShadowDepth="10" BlurRadius="30"/>
            </Border.Effect>

            <Grid>
                <Border Background="{StaticResource WoodBrush}"/>
                <Border Background="#CC050505"/>

                <Grid Margin="20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="85" Height="85" CornerRadius="50" BorderBrush="{StaticResource BronzeBorderBrush}" BorderThickness="1" Background="#080808" Margin="0,0,20,0">
                        <TextBlock Text="👤" Foreground="#333" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Vitality" Foreground="{StaticResource BoneBrush}" FontSize="22" Opacity="0.8"/>
                        <Border BorderBrush="#333" BorderThickness="1" Height="10" CornerRadius="1" Background="#111">
                            <ProgressBar Value="{Binding Vitality}" Maximum="100" BorderThickness="0" Background="Transparent" Foreground="{StaticResource BloodBrush}"/>
                        </Border>

                        <TextBlock Text="Sanity" Foreground="{StaticResource BoneBrush}" FontSize="22" Margin="0,8,0,0" Opacity="0.8"/>
                        <Border BorderBrush="#333" BorderThickness="1" Height="10" CornerRadius="1" Background="#111">
                            <ProgressBar Value="{Binding Sanity}" Maximum="100" BorderThickness="0" Background="Transparent" Foreground="{StaticResource SanityBrush}"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="~" Foreground="{StaticResource BronzeBorderBrush}" FontSize="30" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                        <ItemsControl ItemsSource="{Binding QuickBar}" ItemTemplate="{StaticResource GrimSlotTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                        <TextBlock Text="~" Foreground="{StaticResource BronzeBorderBrush}" FontSize="30" VerticalAlignment="Center" Margin="10,0,0,0" Opacity="0.5"/>
                    </StackPanel>

                    <Button Grid.Column="5" Content="Inventory" Width="120" Height="45" Style="{StaticResource HorrorBtn}" Command="{Binding ToggleInventoryCommand}"/>
                </Grid>
            </Grid>
        </Border>

        <Grid x:Name="InventoryScreen" Visibility="{Binding IsInventoryVisible, Converter={StaticResource BoolToVis}}" Panel.ZIndex="2000" Background="#EE000000">
            <Border Width="800" Height="500" Background="{StaticResource WoodBrush}" BorderBrush="{StaticResource BronzeBorderBrush}" BorderThickness="2" CornerRadius="3">
                <Grid>
                    <Border Background="#F2050505" CornerRadius="3"/>

                    <Grid Margin="30">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="- Inventory -" Foreground="{StaticResource BoneBrush}" FontSize="36" HorizontalAlignment="Center" Opacity="0.9"/>

                        <Grid Grid.Row="1" Margin="0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <ListBox Grid.Column="0" ItemsSource="{Binding FullInventory}" SelectedItem="{Binding SelectedInventoryItem}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Cursor" Value="None"/>
                                        <Setter Property="Margin" Value="8"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="Bd" Background="#111" BorderBrush="#333" BorderThickness="2" CornerRadius="4" Width="90" Height="90">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BronzeBorderBrush}"/>
                                                            <Setter TargetName="Bd" Property="Background" Value="#33E6C898"/>
                                                            <Setter TargetName="Bd" Property="Effect">
                                                                <Setter.Value>
                                                                    <DropShadowEffect Color="#E6C898" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Foreground="#DDD" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" FontFamily="Palatino Linotype" FontSize="14"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Border Grid.Column="1" BorderBrush="#333" BorderThickness="1,0,0,0" Padding="20,0,0,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SelectedInventoryItem}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Hidden"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>

                                            <TextBlock Text="{Binding SelectedInventoryItem.Name}" Foreground="{StaticResource BoneBrush}" FontSize="28" TextWrapping="Wrap"/>
                                            <Border Height="1" Background="#333" Margin="0,5,0,15"/>
                                            <TextBlock Text="{Binding SelectedInventoryItem.Description}" Foreground="#AAA" FontSize="16" FontFamily="Palatino Linotype" TextWrapping="Wrap" Margin="0,0,0,20" FontStyle="Italic"/>
                                            <TextBlock Text="{Binding SelectedInventoryItem.Lore}" Foreground="#888" FontSize="14" FontFamily="Palatino Linotype" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </ScrollViewer>

                                    <WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                                        <Button Content="Equip / Unequip" Width="130" Height="35" Style="{StaticResource HorrorBtn}" FontSize="16" Margin="5"
                        Command="{Binding DataContext.EquipItemCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                        CommandParameter="{Binding SelectedInventoryItem}"/>
                                        <Button Content="Use Item" Width="100" Height="35" Style="{StaticResource HorrorBtn}" FontSize="16" Margin="5"
                        Command="{Binding DataContext.UseItemCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                        CommandParameter="{Binding SelectedInventoryItem}"/>
                                    </WrapPanel>
                                </Grid>
                            </Border>
                        </Grid>

                        <Button Grid.Row="2" Content="Close" Width="140" Height="45" Style="{StaticResource HorrorBtn}" Command="{Binding ToggleInventoryCommand}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="GamePopup" Visibility="{Binding IsPopupVisible, Converter={StaticResource BoolToVis}}" Panel.ZIndex="3000" Background="#EE000000">
            <Border MinWidth="400" MaxWidth="600" MinHeight="200" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource WoodBrush}" BorderBrush="{StaticResource BronzeBorderBrush}" BorderThickness="2" CornerRadius="3">
                <Grid>
                    <Border Background="#EE050505" CornerRadius="3"/>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
                        <TextBlock x:Name="PopupTitle" Text="{Binding PopupTitle}" Foreground="{StaticResource BoneBrush}" FontSize="32" HorizontalAlignment="Center" Opacity="0.9" TextWrapping="Wrap" TextAlignment="Center"/>

                        <Border Height="1" Width="150" Background="{StaticResource BronzeBorderBrush}" Margin="0,15,0,20" Opacity="0.6"/>

                        <TextBlock x:Name="PopupMessage" Text="{Binding PopupMessage}" Foreground="#AAA" FontSize="20" FontFamily="Palatino Linotype" TextWrapping="Wrap" TextAlignment="Center"/>

                        <Button Content="Continue" Width="130" Height="40" Style="{StaticResource HorrorBtn}" Command="{Binding ClosePopupCommand}" Margin="0,30,0,0"/>
                        <Button Content="Stop Hiding" Width="130" Height="40" Command="{Binding StopHidingCommand}" Margin="0,10,0,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource HorrorBtn}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PopupTitle}" Value="HIDING">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Image x:Name="CustomCursor" Source="/Assets/Images/cursor_gauntlet.png" 
               Grid.RowSpan="2"
               Width="80" Height="80" 
               HorizontalAlignment="Left" VerticalAlignment="Top" 
               IsHitTestVisible="False" Panel.ZIndex="9999">
            <Image.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
            </Image.Effect>
        </Image>

    </Grid>
</Window>