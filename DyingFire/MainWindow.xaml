<Window x:Class="DyingFire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DyingFire.ViewModels"
        Title="Dying Fire" Height="720" Width="1080" 
        Background="Black"
        Cursor="None"
        FontFamily="Gabriola">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <ImageBrush x:Key="WoodBrush" ImageSource="/Assets/Images/stylized_wood.png" TileMode="Tile" Viewport="0,0,100,100" ViewportUnits="Absolute"/>

        <LinearGradientBrush x:Key="RoyalGoldBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFED8A" Offset="0.0"/>
            <GradientStop Color="#D4AF37" Offset="0.4"/>
            <GradientStop Color="#AA8822" Offset="0.6"/>
            <GradientStop Color="#FFED8A" Offset="1.0"/>
        </LinearGradientBrush>

        <DropShadowEffect x:Key="GoldGlow" Color="#FFD700" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>

        <SolidColorBrush x:Key="SlotBackground" Color="#88000000"/>

        <Style x:Key="RpgBtn" TargetType="Button">
            <Setter Property="Foreground" Value="#FFED8A"/>
            <Setter Property="FontFamily" Value="Gabriola"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{StaticResource SlotBackground}" 
                                    BorderBrush="{StaticResource RoyalGoldBrush}" 
                                    BorderThickness="2" 
                                    CornerRadius="3">
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#44D4AF37"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Effect" Value="{StaticResource GoldGlow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavOrb" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{StaticResource SlotBackground}" Stroke="{StaticResource RoyalGoldBrush}" StrokeThickness="2"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="#FFED8A" TextBlock.FontSize="28"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Effect" Value="{StaticResource GoldGlow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="VisibleSlotTemplate">
            <Button Click="QuickSlot_Click" Background="Transparent" BorderThickness="0" Margin="5" Cursor="Hand">
                <Grid>
                    <Border Width="60" Height="60" 
                            Background="{StaticResource SlotBackground}" 
                            BorderBrush="{StaticResource RoyalGoldBrush}" 
                            BorderThickness="2" 
                            CornerRadius="2">

                        <TextBlock Text="{Binding Name}" 
                                   Foreground="#DDD" 
                                   FontFamily="Palatino Linotype" 
                                   FontSize="10" 
                                   TextWrapping="Wrap" 
                                   TextAlignment="Center" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"/>
                    </Border>

                    <Border x:Name="SelectBorder" BorderThickness="2" BorderBrush="Transparent" CornerRadius="2"/>
                </Grid>
            </Button>
        </DataTemplate>

    </Window.Resources>

    <Grid MouseMove="Grid_MouseMove">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="140"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Viewbox Stretch="Uniform">
                <Grid Width="900" Height="550">
                    <Image Source="{Binding CurrentLocation.ImagePath}" Stretch="UniformToFill"/>

                    <Border BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="2" IsHitTestVisible="False">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="20"/>
                        </Border.Effect>
                    </Border>

                    <ItemsControl ItemsSource="{Binding CurrentLocation.Interactables}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="{Binding Width}" Height="{Binding Height}" Opacity="0" Cursor="Hand" Click="Interactable_Click"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Content="▲" Style="{StaticResource NavOrb}" Click="MoveNorth_Click"/>
                    <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" Content="▼" Style="{StaticResource NavOrb}" Click="MoveSouth_Click"/>
                    <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0" Content="◀" Style="{StaticResource NavOrb}" Click="MoveWest_Click"/>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0" Content="▶" Style="{StaticResource NavOrb}" Click="MoveEast_Click"/>

                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0">
                        <ListBox ItemsSource="{Binding CurrentLocation.RoomItems}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" Click="RoomItem_Click" Style="{StaticResource RpgBtn}" FontSize="18" Margin="0,4"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
            </Viewbox>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource WoodBrush}" BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="0,2,0,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="90" ShadowDepth="5" BlurRadius="20"/>
            </Border.Effect>

            <Grid Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="90" Height="90" CornerRadius="50" BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="2" Background="Black" Margin="0,0,20,0">
                    <TextBlock Text="👤" Foreground="#555" FontSize="45" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Health" Foreground="#FFED8A" FontSize="22"/>
                    <Border BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="1" Height="12" CornerRadius="2" Background="#33000000">
                        <ProgressBar Value="75" Maximum="100" BorderThickness="0" Background="Transparent" Foreground="#8B0000"/>
                    </Border>

                    <TextBlock Text="Sanity" Foreground="#FFED8A" FontSize="22" Margin="0,8,0,0"/>
                    <Border BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="1" Height="12" CornerRadius="2" Background="#33000000">
                        <ProgressBar Value="40" Maximum="100" BorderThickness="0" Background="Transparent" Foreground="#1E88E5"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="~" Foreground="{StaticResource RoyalGoldBrush}" FontSize="30" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <ItemsControl ItemsSource="{Binding QuickBar}" ItemTemplate="{StaticResource VisibleSlotTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <TextBlock Text="~" Foreground="{StaticResource RoyalGoldBrush}" FontSize="30" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="5" Content="Inventory" Width="120" Height="45" Style="{StaticResource RpgBtn}" Click="Inventory_Click"/>
            </Grid>
        </Border>

        <Grid x:Name="InventoryScreen" Visibility="Collapsed" Panel.ZIndex="2000" Background="#AA000000">
            <Border Width="600" Height="450" Background="{StaticResource WoodBrush}" BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="2" CornerRadius="5">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="40"/>
                </Border.Effect>
                <Grid>
                    <Border Background="#88000000" CornerRadius="3" Margin="2"/>

                    <Grid Margin="30">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="- Backpack -" Foreground="#FFED8A" FontSize="40" HorizontalAlignment="Center" Effect="{StaticResource GoldGlow}"/>

                        <ListBox Grid.Row="1" ItemsSource="{Binding QuickBar}" Background="Transparent" BorderThickness="0" Margin="0,20">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="80" Height="80" Margin="10" Background="{StaticResource SlotBackground}" BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="2" CornerRadius="3">
                                        <TextBlock Text="{Binding Name}" Foreground="#CCC" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" FontFamily="Palatino Linotype"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Grid.Row="2" Content="Close" Width="140" Height="45" Style="{StaticResource RpgBtn}" Click="Inventory_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="GamePopup" Visibility="Collapsed" Panel.ZIndex="3000" Background="#99000000">
            <Border Width="450" Height="250" Background="{StaticResource WoodBrush}" BorderBrush="{StaticResource RoyalGoldBrush}" BorderThickness="2" CornerRadius="5">
                <Grid>
                    <Border Background="#CC000000" CornerRadius="3" Margin="2"/>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                        <TextBlock x:Name="PopupTitle" Text="Found" Foreground="#FFED8A" FontSize="36" HorizontalAlignment="Center" Effect="{StaticResource GoldGlow}"/>

                        <Border Height="2" Width="150" Background="{StaticResource RoyalGoldBrush}" Margin="0,5,0,15" Opacity="0.5"/>

                        <TextBlock x:Name="PopupMessage" Text="..." Foreground="#EEE" FontSize="22" FontFamily="Palatino Linotype" TextWrapping="Wrap" TextAlignment="Center"/>

                        <Button Content="Continue" Width="130" Height="40" Style="{StaticResource RpgBtn}" Click="ClosePopup_Click" Margin="0,25,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Image x:Name="CustomCursor" Source="/Assets/Images/cursor_gauntlet.png" 
               Width="64" Height="64" 
               HorizontalAlignment="Left" VerticalAlignment="Top" 
               IsHitTestVisible="False" Panel.ZIndex="9999">
            <Image.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2"/>
            </Image.Effect>
        </Image>

    </Grid>
</Window>